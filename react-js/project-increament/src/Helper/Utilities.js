export function dd( x, heading ) {

    heading = heading || '';

    if( heading ) {
        console.log( heading, x ); return;
    }

    console.log( x );
    return;
}

export function api_response()
{
    //return await axios.
}
